package;

/**
 * A class for storing some static, public, global variables (maybe functions, too).
 * This is mostly here for the menu modifications I'm making.
 */
class ModGlobals
{
  public static var currentDifficulty:Int = 1; // the global selected difficulty for menus, defaults to normal
}
